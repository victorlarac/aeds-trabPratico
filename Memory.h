#ifndef MEMORY_H
#define MEMORY_H

#include <stdio.h>
#include <stdlib.h>

#include "Swap.h"
#include "Lista.h"


void MemoryInitialize(int num, int memory [num],[Max]);
int Queue(char *path, int num);
void Dequeue(char *path, Pointers *p);



#endif